nav.navbar.navbar-default
  .container-fluid
    .navbar-header
      a(href="/").navbar-brand Todo Manager

    ul.nav.navbar-nav  
      if !session 
        li 
          a(href="/register") Register
      else 
        li
          p Hi #{user.firstname} !
        li
          a(href="/todos") Todos
        li
          a(href="/account") Account
      li
        a(href=session ? '/logout' : '/login')= session ? 'Logout' : 'Login'